# ⚡ Supabaseクラウド同期 - 5分セットアップ

## 🚀 超簡単セットアップ（5分）

### 1️⃣ Supabaseプロジェクト作成（2分）
1. [Supabase](https://supabase.com) にアクセス
2. **「Start your project」**をクリック
3. GitHubでサインアップ/ログイン
4. **「New project」**をクリック
5. プロジェクト名：`ai-chat`
6. パスワード：適当に設定（覚えなくてもOK）
7. **「Create new project」**をクリック

### 2️⃣ 環境変数取得（1分）  
プロジェクト作成後、左サイドバーの⚙️**Settings** → **API**:
- **Project URL**: `https://xxxxx.supabase.co` をコピー
- **anon public**: `eyJhbGci...` をコピー

### 3️⃣ 環境変数設定（1分）
プロジェクトルートの `.env.local` ファイルに追加：

```env
# Supabaseクラウド同期設定
NEXT_PUBLIC_SUPABASE_URL=https://your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...your-anon-key...
```

### 4️⃣ データベース設定（1分）
Supabaseダッシュボードで**SQL Editor**を開き、以下を実行：

```sql
-- 設定テーブル
create table user_settings (
  id bigint generated by default as identity primary key,
  user_id uuid references auth.users not null,
  settings jsonb not null,
  updated_at timestamp with time zone default now(),
  unique(user_id)
);

-- RLS有効化
alter table user_settings enable row level security;
create policy "Users can manage own settings" on user_settings for all using (auth.uid() = user_id);
```

### 5️⃣ 開発サーバー再起動
```bash
# 開発サーバー停止（Ctrl+C）
npm run dev
```

## ✅ 完了確認
1. アプリで**「⚙️設定」**→**「☁️クラウド同期」**をクリック
2. メールアドレス入力フィールドが表示される
3. メールアドレス入力→認証メール送信成功

## 🎉 利用開始
- メールで送信されたリンクをクリック
- 自動的にログイン完了
- **「データを同期」**ボタンでクラウド同期開始

## 💡 トラブルシューティング

### メール入力できない場合
- 開発サーバー再起動（環境変数反映のため）
- ブラウザのキャッシュクリア

### 認証メールが届かない場合  
- 迷惑メールフォルダを確認
- Gmail等の「プロモーション」タブを確認

### データベースエラーの場合
- SQLの実行をもう一度確認
- Supabaseプロジェクトの地域設定確認

## 🔐 セキュリティ
- ✅ APIキーはローカル保存のみ（クラウド同期対象外）
- ✅ Row Level Security有効
- ✅ ユーザー分離・暗号化
- ✅ いつでも退会・データ削除可能

## 💰 料金
- 無料枠：500MB・50,000リクエスト/月
- 通常使用では無料枠で十分